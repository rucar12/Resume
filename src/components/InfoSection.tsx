import styles from './InfoSection.module.scss';

const InfoSection = ({children, title, icon}) => {

    return (
        <div>
            {children}
        </div>
    )
}

export default InfoSection;